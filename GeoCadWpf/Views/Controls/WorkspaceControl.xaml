<UserControl
    x:Class="GeoCadWpf.Views.Controls.WorkspaceControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:GeoCadWpf.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:GeoCadWpf.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*" MinWidth="150" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" MinWidth="100" />
        </Grid.ColumnDefinitions>

        <Border
            Margin="0,3,0,3"
            BorderBrush="{DynamicResource InactiveStrokeBrush}"
            BorderThickness="1"
            CornerRadius="3.5">

            <controls:ViewportControl ViewportController="{Binding ViewportController}" />

        </Border>

        <GridSplitter
            Grid.Column="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent"
            ResizeBehavior="PreviousAndNext"
            ShowsPreview="False" />


        <Grid Grid.Column="2" Margin="0,3,3,3">

            <Grid.RowDefinitions>
                <RowDefinition Height="3*" MinHeight="25" />
                <RowDefinition Height="5" />
                <RowDefinition Height="2*" MinHeight="25" />
            </Grid.RowDefinitions>

            <controls:ObjectTreeControl
                x:Name="GeoTree"
                ItemsSource="{Binding Nodes}"
                SelectedItem="{Binding SelectedObject, Mode=TwoWay}" />

            <GridSplitter
                Grid.Row="1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="Transparent"
                ResizeBehavior="PreviousAndNext"
                ShowsPreview="False" />

            <controls:PropertiesControl Grid.Row="2" Item="{Binding SelectedObject, Mode=TwoWay}" />

        </Grid>

    </Grid>

</UserControl>
