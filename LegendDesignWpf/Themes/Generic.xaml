<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:LegendDesignWpf.Converters"
    xmlns:ldc="clr-namespace:LegendDesignWpf.Controls">

    <converters:StringLengthToVisibilityConverter x:Key="StringLengthToVisibilityConverter" />

    <Style x:Key="FocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle
                        Margin="2"
                        SnapsToDevicePixels="true"
                        Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                        StrokeDashArray="1 2"
                        StrokeThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="OptionMarkFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle
                        Margin="14,0,0,0"
                        SnapsToDevicePixels="true"
                        Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                        StrokeDashArray="1 2"
                        StrokeThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ldc:LDWindow}">
        <Setter Property="Background" Value="#FFFFFF" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ldc:LDWindow}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <AdornerDecorator>
                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </AdornerDecorator>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ldc:NavigationView}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ldc:NavigationView}">
                    <Grid>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <ListBox
                            x:Name="PART_NavigationItems"
                            ItemsSource="{TemplateBinding NavigationItems}"
                            SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <ldc:NavButton>
                                        <ldc:PackIcon Width="20" Kind="Home" />
                                    </ldc:NavButton>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <ContentPresenter Grid.Column="1" Content="{TemplateBinding Content}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ldc:LDPage}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Title" Value="Legend Title" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ldc:LDPage}">
                    <Grid Margin="20,10,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!--  // HEADER //  -->
                            <Grid Margin="0,0,47,15">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!--  // Page Title //  -->
                                <TextBlock Style="{StaticResource PageTitleTextBlockStyle}" Text="{TemplateBinding Title}" />

                                <!--  // Header Content //  -->
                                <ContentPresenter
                                    Grid.Column="1"
                                    Height="33"
                                    Content="{TemplateBinding HeaderContent}" />

                            </Grid>

                            <AdornerDecorator Grid.Row="2">
                                <ContentPresenter />
                            </AdornerDecorator>

                        </Grid>

                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/TextBlockStyles.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/UserControlStyles.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/ButtonsStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/ListBoxStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/ScrollViewerStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/PageStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/PopupStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/ProgressBarStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/SliderStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/BorderStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/TextBoxStyle.xaml" />
        <ResourceDictionary Source="/LegendDesignWpf;component/Styles/HueSliderStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

</ResourceDictionary>
