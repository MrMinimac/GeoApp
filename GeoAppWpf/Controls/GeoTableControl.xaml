<UserControl
    x:Class="GeoAppWpf.Controls.GeoTableControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:GeoAppWpf.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid Grid.Row="1" Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="100" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" MinWidth="100" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" MinWidth="100" />
        </Grid.ColumnDefinitions>

        <!--  Буровые линии  -->
        <GroupBox Grid.Column="0">

            <GroupBox.Header>
                <TextBlock Style="{StaticResource LabelSmallTextStyle}" Text="Буровые Линии" />
            </GroupBox.Header>

            <DataGrid
                x:Name="BoreholeLinesDataGrid"
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                AutoGenerateColumns="True"
                AutoGeneratingColumn="BoreholeLinesDataGrid_AutoGeneratingColumn"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserResizeRows="False"
                HeadersVisibility="Column"
                IsReadOnly="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectionChanged="BoreholeLinesDataGrid_SelectionChanged"
                SelectionMode="Single"
                SelectionUnit="FullRow" />

        </GroupBox>

        <GridSplitter
            Grid.Column="1"
            Width="5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent"
            ResizeBehavior="PreviousAndNext"
            ShowsPreview="False" />

        <!--  Скважины  -->
        <GroupBox Grid.Column="2">

            <GroupBox.Header>
                <TextBlock Style="{StaticResource LabelSmallTextStyle}" Text="Скважины" />
            </GroupBox.Header>

            <DataGrid
                x:Name="BoreholesDataGrid"
                AutoGenerateColumns="True"
                AutoGeneratingColumn="BoreholesDataGrid_AutoGeneratingColumn"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserResizeRows="False"
                HeadersVisibility="Column"
                IsReadOnly="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectionChanged="BoreholesDataGrid_SelectionChanged"
                SelectionMode="Single"
                SelectionUnit="FullRow" />

        </GroupBox>

        <GridSplitter
            Grid.Column="3"
            Width="5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent"
            ResizeBehavior="PreviousAndNext"
            ShowsPreview="False" />

        <!--  Пробы  -->
        <GroupBox Grid.Column="4">

            <GroupBox.Header>
                <TextBlock Style="{StaticResource LabelSmallTextStyle}" Text="Пробы" />
            </GroupBox.Header>

            <DataGrid
                x:Name="SamplesDataGrid"
                AutoGenerateColumns="True"
                AutoGeneratingColumn="SamplesDataGrid_AutoGeneratingColumn"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserResizeRows="False"
                HeadersVisibility="Column"
                IsReadOnly="True"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectionMode="Single"
                SelectionUnit="FullRow" />

        </GroupBox>

    </Grid>

</UserControl>
